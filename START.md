# 啟動指南

## 配置說明

### 網路配置
- **後端服務器**: 綁定到 `0.0.0.0:3000` (所有網路介面)
- **外部訪問地址**: `http://123.194.35.219:3000`
- **本地訪問地址**: `http://localhost:3000`

### 啟動步驟

1. **安裝依賴**:
   ```bash
   yarn install:all
   ```

2. **啟動開發服務器**:
   ```bash
   yarn dev
   ```

3. **檢查服務器狀態**:
   - 本地測試: http://localhost:3000
   - 外部訪問: http://123.194.35.219:3000

### 防火牆設置

確保您的 Windows 防火牆允許端口 3000 和 8080：

1. 打開 Windows Defender 防火牆
2. 點擊「允許應用或功能通過 Windows Defender 防火牆」
3. 添加端口規則：
   - 端口 3000 (後端 API)
   - 端口 8080 (前端開發服務器)

### 故障排除

如果仍然無法從外部訪問：
1. 檢查路由器是否有端口轉發設置
2. 確認 ISP 沒有封鎖這些端口
3. 檢查網路適配器設置

### 訪問地址

- **前端**: http://localhost:8080 (本地) 或 http://123.194.35.219:8080 (外部)
- **後端 API**: http://localhost:3000 (本地) 或 http://123.194.35.219:3000 (外部)
- **反向代理**: Docker 佈署時可直接透過 http://localhost:8080/api/* 存取 API，伺服器會透過 Nginx 轉發到後端服務。
