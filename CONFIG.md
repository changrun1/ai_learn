# 配置文件說明

## 統一配置結構

本專案已將前後端配置統一到根目錄的 `.env` 文件中。

### 配置優先級

1. **根目錄 `.env`** - 主要配置文件，包含所有共用配置
2. **server/.env** - 後端特定配置，會覆蓋根目錄的相同配置
3. **app/.env** - 前端特定配置
4. **app/.env.development** - 前端開發環境配置

### 重要配置說明

- **SERVER_HOST**: 後端服務器主機地址，設置為您的 IP `123.194.35.219`
- **SERVER_PORT**: 後端服務器端口，默認 `3000`
- **API_BASE_URL**: 前端調用 API 的基礎 URL，指向後端服務器
- **FRONTEND_URL**: 後端 CORS 允許的前端 URL

### 使用方式

1. **開發環境啟動**：
   ```bash
   yarn dev
   ```

2. **生產環境啟動**：
   ```bash
   yarn start
   ```

3. **檢查配置**：
   ```bash
   yarn env:check
   ```

### 注意事項

- 修改配置後需要重新啟動服務
- 生產環境部署時，確保 IP 地址和端口配置正確
- CORS 配置已設置為允許前端地址訪問
